import React, { FC } from 'react';
import { useAppSelector } from 'app/hooks';
import { selectIsFuriganaEnabled } from './slice';

// export const Furigana: FC<{text: string}> = ({text, children})=> {
//     const enabled = useAppSelector(selectIsFuriganaEnabled);

//     <
// }
